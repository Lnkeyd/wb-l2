const TOP_TEXT = {
    yOffset: 0,
    xOffset: 0,
    fontSize: 16,
    fontWeight: 400,
    fontColor: '#ffffff',
    fontFamily: "Times New Roman",
    fontStroke: false,
    textValue: ''
}

const BOTTOM_TEXT = JSON.parse(JSON.stringify(TOP_TEXT))